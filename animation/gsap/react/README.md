## react/

이 폴더는 **React 방식 GSAP 예제**를 위해, `gsap` / `@gsap/react`를 **npm으로 관리**합니다.

### 실행

```bash
cd react
npm install
npm run dev
```

### 참고

- **GSAP는 npm에서 ES modules로 제공**됩니다.
- React에서는 `@gsap/react`의 `useGSAP()`를 쓰면 cleanup/스코핑이 편해집니다.


